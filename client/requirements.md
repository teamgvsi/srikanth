## Packages
framer-motion | Smooth scroll animations and page reveals
lucide-react | Icons for stats and buttons
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms
zod | Schema validation

## Notes
Tailwind config is assumed to use CSS variables for font families (--font-sans, --font-serif).
Images are imported statically using Vite aliases (@assets).
